[user]
	email = apohllo@o2.pl
	name = Aleksander Smywinski-Pohl
[alias]
	br = branch -a
	co = checkout
	ci = commit
	lg = log -p
	lol = log --graph --decorate --pretty=oneline --abbrev-commit
	lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
	ls = ls-files
	st = status
        dc = diff --cached
        pr = pull --rebase github master
[color]
	branch = auto
	diff = auto
	status = auto
[push]
	default = matching
[pull]
        ff = true
	rebase = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
